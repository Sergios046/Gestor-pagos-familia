/** Central keys — mirror table names for a future DB migration */
export const STORAGE_KEYS = {
  EXPENSES: "gestor_pagos:expenses",
  DEBTS: "gestor_pagos:debts",
  /** Schema version for migrations from localStorage */
  META: "gestor_pagos:meta",
};
